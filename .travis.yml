language: python

sudo: false

python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - 3.5

install:
  - pip install pytest mock coveralls pytest-cov

script:
  - py.test --cov echo test_echo.py

after_success:
  - coveralls
